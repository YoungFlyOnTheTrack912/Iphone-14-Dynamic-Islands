{
  "name": "simple-web-proxy",
  "version": "1.0.0",
  "description": "A minimal web proxy that rewrites HTML so pages load under proxy origin (bookmarklets allowed)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "vercel dev",
    "vercel-build": "echo \"No build needed\""
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "compression": "^1.8.1",
    "express": "^4.18.2",
    "node-cache": "^5.1.2",
    "node-fetch": "^2.6.12"
  },
  "engines": {
    "node": "24.x"
  }
}
